# =============================================================================
# VYBES API - ENVIRONMENT VARIABLES EXAMPLE
# =============================================================================
# Copy this file to .env and fill in your actual values
# For Railway deployment, set these variables in Railway dashboard

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Server Configuration
PORT=:8080
DB_NAME=vybes_production

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================

# JWT Configuration (REQUIRED - Generate a strong secret)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Wallet Encryption (REQUIRED - Generate a strong key for wallet encryption)
WALLET_ENCRYPTION_KEY=your-wallet-encryption-key-here-32-bytes-minimum

# =============================================================================
# DATABASE CONFIGURATION (MongoDB)
# =============================================================================

# MongoDB Connection (Railway will provide this automatically)
MONGO_URI=mongodb://mongodb:27017

# MongoDB Root Credentials (Set in Railway dashboard)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-mongodb-root-password

# =============================================================================
# CACHE CONFIGURATION (Redis)
# =============================================================================

# Redis Connection (Railway will provide this automatically)
REDIS_ADDR=redis:6379

# Redis Authentication (Set in Railway dashboard)
REDIS_PASSWORD=your-redis-password

# Redis Database Number
REDIS_DB=0

# =============================================================================
# OBJECT STORAGE CONFIGURATION (MinIO)
# =============================================================================

# MinIO Connection (Railway will provide this automatically)
MINIO_ENDPOINT=minio:9000

# MinIO Credentials (Set in Railway dashboard)
MINIO_ACCESS_KEY=your-minio-access-key
MINIO_SECRET_KEY=your-minio-secret-key

# MinIO Configuration
MINIO_USE_SSL=true
MINIO_POSTS_BUCKET=vybes-posts
MINIO_STORIES_BUCKET=vybes-stories

# =============================================================================
# MESSAGE QUEUE CONFIGURATION (NATS)
# =============================================================================

# NATS Connection (Railway will provide this automatically)
NATS_URL=nats://nats:4222

# =============================================================================
# EXTERNAL SERVICES
# =============================================================================

# Email Service (Resend) - REQUIRED
RESEND_API_KEY=re_your_resend_api_key_here
SENDER_EMAIL=noreply@yourdomain.com

# Ethereum Blockchain Configuration - REQUIRED
ETH_RPC_URL=https://mainnet.infura.io/v3/your-infura-project-id
# Alternative RPC URLs:
# ETH_RPC_URL=https://eth-mainnet.alchemyapi.io/v2/your-alchemy-key
# ETH_RPC_URL=https://rpc.ankr.com/eth/your-ankr-key

# =============================================================================
# ENVIRONMENT-SPECIFIC CONFIGURATIONS
# =============================================================================

# Development Environment
# DB_NAME=vybes_development
# MINIO_USE_SSL=false
# ETH_RPC_URL=https://sepolia.infura.io/v3/your-infura-project-id

# Staging Environment
# DB_NAME=vybes_staging
# MINIO_USE_SSL=true
# ETH_RPC_URL=https://sepolia.infura.io/v3/your-infura-project-id

# Production Environment
# DB_NAME=vybes_production
# MINIO_USE_SSL=true
# ETH_RPC_URL=https://mainnet.infura.io/v3/your-infura-project-id

# =============================================================================
# OPTIONAL CONFIGURATIONS
# =============================================================================

# Logging Level (optional)
# LOG_LEVEL=info

# CORS Configuration (optional)
# CORS_ORIGIN=https://your-frontend-domain.com

# Rate Limiting (optional)
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW=15m

# =============================================================================
# RAILWAY-SPECIFIC NOTES
# =============================================================================
#
# For Railway deployment:
# 1. Set these variables in Railway dashboard under Variables tab
# 2. Railway automatically provides service URLs for:
#    - MONGO_URI (points to mongodb service)
#    - REDIS_ADDR (points to redis service)
#    - MINIO_ENDPOINT (points to minio service)
#    - NATS_URL (points to nats service)
#
# 3. You must manually set:
#    - JWT_SECRET
#    - WALLET_ENCRYPTION_KEY
#    - RESEND_API_KEY
#    - SENDER_EMAIL
#    - ETH_RPC_URL
#    - MONGO_ROOT_USERNAME
#    - MONGO_ROOT_PASSWORD
#    - REDIS_PASSWORD
#    - MINIO_ACCESS_KEY
#    - MINIO_SECRET_KEY
#
# 4. Generate secure secrets:
#    - JWT_SECRET: Use a strong random string (32+ characters)
#    - WALLET_ENCRYPTION_KEY: Use a strong random string (32+ characters)
#    - Database passwords: Use strong passwords
#
# 5. Example of generating secure secrets:
#    openssl rand -base64 32  # For JWT_SECRET
#    openssl rand -base64 32  # For WALLET_ENCRYPTION_KEY
#    openssl rand -base64 16  # For database passwords
#
# =============================================================================